name: Belajar CI
on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v4
    - name: Run a one liner
      run: docker build -t myapp:1.0 .
